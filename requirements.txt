# Core dependencies
aiohttp>=3.9.0
websockets>=12.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0

# Performance optimization
uvloop>=0.19.0  # High-performance event loop
orjson>=3.9.10  # Fast JSON parsing

# Optional but recommended
numpy>=1.24.0  # For vectorized calculations if needed
redis>=5.0.1  # For caching if needed later
